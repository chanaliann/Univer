#pragma once

/*функция заполнения матрицы*/
void inputFunc(int*, int, int);

/*функция вывода матрицы на экран*/
void outputFunc(int*, int, int);

/*функция подсчета отрицательных элементов в матрице*/
void countEl(int*, int, int);