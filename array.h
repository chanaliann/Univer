#pragma once

/*функция ввода массива*/
void inputArr(int*, int);

/*функция вывода массива*/
void outputArr(int*, int);

/*функция подсчета отрицательных элементов в массиве*/
void countElA(int*, int);